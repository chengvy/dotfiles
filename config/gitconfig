[credential]
	helper = manager-core
[user]
	name = Chengvy
	email = chengvy@outlook.com
[core]
	editor = nvim
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[alias]
	logg = log --graph --color=always --decorate --date=short --date-order --pretty='%C(auto)%h %C(green)%an %C(cyan)%ad%C(auto)%d %s'
[diff]
	tool = nvim -d
